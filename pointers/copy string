#include<stdio.h>
#include<string.h>
int main(){
    char str1[100];
    char str2[100];
    int i=0;
    fgets(str1,sizeof str1,stdin);
    char *p=str1;
    char *s=str2;
    for(i=0;*(p+i)!='\0';i++){
       *(s+i)=*(p+i);

    }
    printf("%s",s);
   
}
