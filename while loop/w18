#include <stdio.h>
int main() {
    int cap, n, i = 0,x,
      safe = 0, fail = 0;
    scanf("%d%d", &cap, &n);
    while (i < n) {
        scanf("%d", &x);
        if (x > cap) 
        fail++;
        else 
        safe++;
        i++;
    }
    printf("%d%d", safe, fail);
    return 0;
}
