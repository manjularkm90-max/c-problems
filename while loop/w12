#include <stdio.h>
int main() {
    int n, i = 0, x;
    int max = 0;
    int surge = 0;
    scanf("%d", &n);
    while (i < n) {
        scanf("%d", &x);
        if (x > max) max = x;
        if (x > 5) 
        surge++;
        i++;
    }
    printf("%d%d", max, surge);
}
