#include <stdio.h>

int main() {
    int n = 13;
    int binary[32];
    int i = 0;

    while (n > 0) {
        binary[i] = n % 2;
        n /= 2;
        i++;
    }

    while (i > 0) {
        printf("%d", binary[--i]);
    }

    return 0;
}
