#include <stdio.h>
int main() {
    int n, i = 0, x;
    int  total = 0;
    int  delayed = 0;
    scanf("%d", &n);
    while (i < n) {
        scanf("%d", &x);
        total+=x;
        if (x>2)
         delayed++;
        i++;
    }
    printf("%d%d", total, delayed);
}
