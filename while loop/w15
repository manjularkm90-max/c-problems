#include <stdio.h>
int main() {
    int n, i = 0, x,
    total = 0;
     int burnout = 0;
    scanf("%d", &n);
    while (i < n) {
        scanf("%d", &x);
        total += x;
        if (x > 4) 
        burnout++;
        i++;
    }
    printf("%d%d", total, burnout);
}
